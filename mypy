#!/usr/bin/env python3
"""
mypy用スラッシュコマンド
Usage: /mypy [options]
"""

import sys
import subprocess
from pathlib import Path

def main():
    # プロジェクトルートディレクトリに移動
    script_dir = Path(__file__).parent
    
    # quality_check.py --tool mypyを実行
    cmd = [sys.executable, "quality_check.py", "--tool", "mypy"] + sys.argv[1:]
    
    try:
        result = subprocess.run(cmd, cwd=script_dir)
        sys.exit(result.returncode)
    except Exception as e:
        print(f"Error: {e}")
        sys.exit(1)

if __name__ == "__main__":
    main()